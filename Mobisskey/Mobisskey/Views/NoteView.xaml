<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Mobisskey.Views.NoteView"
             Padding="8"
             Orientation="Horizontal">
    <Image Source="{Binding Icon}" x:Name="image" WidthRequest="64" HeightRequest="64" Margin="0,0,16,0"/>
    <StackLayout>
        <Label IsVisible="{Binding IsRenote}">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="♺ " TextColor="#4CAF50"/>
                    <Span Text="{Binding RenoteUserName}" FontAttributes="Bold" FontSize="16"/>
                    <Span Text=" さんがRenote" FontSize="14"/>
                </FormattedString>
            </Label.FormattedText>
        </Label>
        <StackLayout Margin="0,0,0,4" Orientation="Horizontal">
            <Label Text="{Binding ScreenName}" FontSize="15" TextColor="Black"/>
            <Label Text="{Binding UserName}" FontSize="12" TextColor="Gray"/>
        </StackLayout>
        <Label Text="{Binding Text}" FontSize="13" TextColor="Black"/>
    </StackLayout>
</StackLayout>